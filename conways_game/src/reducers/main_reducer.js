


const initial_state = {
    rows: 25,
    cols: 25,
    bg_color: 'white',
    accent_color: 'black',
    preset_grid: 'none',

};

export function Main_Reducer(state = initial_state, action) {
    switch (action.type) {
        default:
            return state
    }
}